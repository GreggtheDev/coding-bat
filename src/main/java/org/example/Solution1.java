// Define the function sleepIn which determines if we can sleep in based on two conditions
public boolean sleepIn(boolean weekday, boolean vacation) {

    // Return true if it is not a weekday or if we are on vacation
    // !weekday: This checks if it is NOT a weekday. If weekday is true, !weekday will be false and vice versa.
    // || vacation: This checks if we are on vacation. If either condition is true, the result will be true.
    // return !weekday || vacation;
}


